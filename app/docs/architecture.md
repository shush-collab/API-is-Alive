# Architecture

`apps/fake-api` is the demo upstream service. `apps/gateway` is the gateway scaffold. `apps/worker` is for async scoring. `apps/replay` stores traffic scenarios. `apps/dashboard` is the UI shell. `packages/shared` holds shared TypeScript types.
